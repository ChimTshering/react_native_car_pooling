import { ScrollView, Text, TextInput, View } from "react-native";

export const Profile = ()=>{
  return (
    <View>
      <ScrollView>
        <TextInput placeholder="Hit me"> </TextInput>
        <Text>hork harder</Text>
      </ScrollView>
    </View>
  );
}